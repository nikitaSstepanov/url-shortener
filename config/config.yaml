server:
    url: ":80"

logger:
    level: "info"
    add_source: true
    is_json: true
    set_default: true

postgres:
    host: "url_shortener_postgres"
    port: "5432"
    username: "admin"
    dbname: "postgres"
    sslmode: "disable"

redis:
    host: "url_shortener_redis"
    port: "6379"
    db: "1"